language: node_js
node_js:
  - lts/*
script:
  - npm run docs:build
  #  npm run deploy
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: docs/.vuepress/dist
  github-token: $travisCliToken
  keep-history: true
  on:
    branch: master

